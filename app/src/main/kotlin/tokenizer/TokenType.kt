package tokenizer

enum class TokenType {
    EOF,
    STRING_UNTERMINATED,
    UNEXPECTED_CHAR,

    COMMENT,
    STRING,
    AND, VAR, CLASS, ELSE, FALSE, FOR, FUN, IF, NIL, OR, PRINT, RETURN, SUPER, THIS, TRUE, WHILE,
    IDENTIFIER,
    NUMBER,
    SPACE,
    SLASH,
    NEWLINE,
    BANG,
    BANG_EQUAL,
    EQUAL_EQUAL,
    LESS, GREATER,
    LESS_EQUAL, GREATER_EQUAL,
    EQUAL,
    COMMA,
    DOT,
    MINUS,
    PLUS,
    STAR,
    LEFT_PAREN,
    RIGHT_PAREN,
    LEFT_BRACKET,
    RIGHT_BRACKET,
    LEFT_BRACE,
    RIGHT_BRACE,
    SEMICOLON,
}